Input formatting
=================

This module has the functions needed to format the input DataFrame into the data that we want to show in Dash. For instance, given a DataFrame with tweets you can create a network of retweets among users.

.. automodule:: generate_utils
